extends ./layout
block content
  .container
     .row
        .col-md-2
           h1 首页
     .row
       each item in movies
          .col-md-2
             .thumbnail
                a(href="/movie/#{item._id}")
                   img(src="#{item.poster}",alt="#{item.title}")
                .caption
                    h3 #{item.title}
                    p:a.btn.btn-primary(href="",role="button")
                      |观看预告片